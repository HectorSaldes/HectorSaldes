const Badge = ({ text }) => (
	<strong className="border text-base text-stone-700 dark:text-stone-400 border-current px-5 py-1.5 rounded-full tracking-wide">
		{text}
	</strong>
);

export default Badge;